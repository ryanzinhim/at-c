@page
@model EXPEXturism.Pages.CRUDReserva.EditModel

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>Reserva</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Reserva.Id" />
            <div class="form-group">
                <span class="text-danger">*</span>
                <label asp-for="Reserva.ClienteId" class="control-label">Nome do Cliente</label>
                <select asp-for="Reserva.ClienteId" class="form-control" asp-items="ViewBag.ClienteId"></select>
                <span asp-validation-for="Reserva.ClienteId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <span class="text-danger">*</span>
                <label asp-for="Reserva.PacoteId" class="control-label">Nome do Pacote</label>
                <select asp-for="Reserva.PacoteId" class="form-control" asp-items="ViewBag.PacoteId"></select>
                <span asp-validation-for="Reserva.PacoteId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Reserva.DataReserva" class="control-label">Data do Evento</label>
                <input asp-for="Reserva.DataReserva" class="form-control" readonly/>
                <span asp-validation-for="Reserva.DataReserva" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="./Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
